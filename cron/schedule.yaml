# ═══════════════════════════════════════════════
# 夸父定时任务配置
# ═══════════════════════════════════════════════
# schedule 支持: "30m" / "2h" / "0 8 * * *" (每日8点) / ISO时间

tasks:
  # ── 所有静态任务已取消 ────────────────────────────
  # 所有 cron 任务改为用户通过飞书/微信对话动态创建，
  # 创建时自动记录 source_channel 的 chat_id 作为推送目标。
  # 参见创建 cron 的 API: POST /api/cron/create
  # 和 agent_loop 中的 create_cron_tool。

